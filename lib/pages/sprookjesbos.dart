import 'package:flutter/material.dart';

class SprookjesbosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Page two')),
      body: Center(child: Text('Welcome to Page two!')),
    );
  }
}
